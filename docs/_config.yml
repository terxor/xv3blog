collections:
  authors:
    output: true

defaults:
  - scope:
      path: ""
      type: "authors"
    values:
      layout: "author"
  - scope:
      path: ""
      type: "posts"
    values:
      layout: "post"
  - scope:
      path: ""
    values:
      layout: "default"

url: https://terxor.github.io
baseurl: /xv3blog

markdown: kramdown
kramdown:
  math-engine: katex
  input: GFM
  syntax_highlighter: rouge
